---
- name: Adding custom Ansible fact on hosts in the database host group
  hosts: database
  become: true
  
  tasks:
    - name: Create custom facts directory
      ansible.builtin.file:
        path: /etc/ansible/facts.d
        state: directory
        owner: root
        group: root
        mode: '0755'

    - name: Deploy custom fact file with server_role=mysql fact
      ansible.builtin.copy:
        dest: /etc/ansible/facts.d/custom.fact
        content: '{ "sample_exam": { "server_role": "mysql" } }'
        owner: root
        group: root
        mode: '0644'

    - name: Re-read facts after deploying new fact file
      ansible.builtin.setup:
        filter: ansible_local

    - name: Display custom facts
      ansible.builtin.debug:
        msg: "{{ ansible_local.custom }}"